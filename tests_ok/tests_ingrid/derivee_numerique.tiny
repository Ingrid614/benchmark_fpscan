/* Benchmark : Dérivée numérique */
/* Cancellation dans f(x+h) - f(x) */

float x, h;
float f_x, f_xh;
float derivee;

x = 1.0;
h = 0.001;  /* Pas très petit */

/* f(x) = cos(x) */
f_x = cos(x);
f_xh = cos(x + h);

derivee = (f_xh - f_x) / h;
